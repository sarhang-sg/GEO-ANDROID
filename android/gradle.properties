org.gradle.jvmargs=-Xmx6G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=false
android.nonTransitiveRClass=true
android.nonFinalResIds=true
kotlin.code.style=official
# Flutter 3.47.1 still reads the legacy Android variants bridge. AGP 9 built-in
# Kotlin remains enabled; this only selects the compatible Android DSL bridge.
android.newDsl=false
